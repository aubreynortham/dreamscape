class ExploreController < ApplicationController
  # i might just add this to the dreams controller rather than having a controller with one method that just renders a view, but i also might not (i can see the merits of either really)
  def get_explore
  end

end
